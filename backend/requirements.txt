# FastAPI and server dependencies
fastapi==0.109.0
uvicorn[standard]==0.25.0
pydantic==2.5.0
pydantic-settings==2.0.3
python-multipart==0.0.6

# Quantum computing libraries
qiskit>=1.0.2
# Use the latest compatible version of qiskit-aer
qiskit-aer==0.17.0 --only-binary=qiskit-aer

# Scientific computing
numpy==1.26.0

# For environment variable management
python-dotenv==1.0.0

# Note: Starlette is included as a dependency of FastAPI
# Don't specify Starlette version separately as it conflicts with FastAPI's requirements